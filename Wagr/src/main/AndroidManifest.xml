<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.codeclan.com.wagr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/wagr_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewResolvedBetActivity"/>
        <activity android:name=".ResolvedBetsActivity" />
        <activity android:name=".UserListActivity" />
        <activity android:name=".NewBetActivity" />
        <activity android:name=".UserCRUDActivity" />
        <activity android:name=".BetCRUDActivity" />
        <activity android:name=".NewUserActivity"></activity>
    </application>

</manifest>