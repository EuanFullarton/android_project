<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_new_user"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="example.codeclan.com.wagr.NewUserActivity">

    <EditText
        android:id="@+id/new_user_name"
        android:hint="Enter name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignLeft="@+id/submit_new_user"
        android:layout_alignStart="@+id/submit_new_user"
        android:layout_marginTop="103dp" />

    <Button
        android:id="@+id/submit_new_user"
        android:onClick="submitNewUserButtonClicked"
        android:text="Save new user"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="74dp"
        android:layout_below="@+id/new_user_name"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
